# Example environment variables for deployment

# Port (PaaS will usually set this)
PORT=5000

# Flask debug (0 or 1)
FLASK_DEBUG=0

# CORS origin for frontend (set to frontend URL in production)
FRONTEND_ORIGIN=*

# Similarity threshold used for match decisions (0..1)
MATCH_THRESHOLD=0.6
